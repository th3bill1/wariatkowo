import { useEffect, useState } from "react"; import { RefreshControl, ScrollView, Text } from "react-native"; import type { CalendarEvent, ShoppingItem, Task, TaskStats } from "../../../../shared/models"; import { useAuth } from "../../src/AuthProvider"; import { cached } from "../../src/cache"; import { Card, Title, s } from "../../src/ui";
const day=(d:Date)=>d.toISOString().slice(0,10);
export default function Dashboard(){const {member,api,logout}=useAuth(); const [data,setData]=useState<{tasks:Task[];shopping:ShoppingItem[];events:CalendarEvent[];stats:TaskStats|null}>({tasks:[],shopping:[],events:[],stats:null}),[loading,setLoading]=useState(true),[stale,setStale]=useState(false); const load=async()=>{setLoading(true); const end=new Date();end.setDate(end.getDate()+14); const value=await cached("dashboard",async()=>{const [tasks,shopping,events,stats]=await Promise.all([api.tasks.list(),api.shopping.list(),api.calendar.list(day(new Date()),day(end)),api.tasks.stats().catch(()=>null)]);return{tasks,shopping,events,stats}});setData(value.data);setStale(value.stale);setLoading(false)}; useEffect(()=>{void load()},[]); const open=data.tasks.filter(x=>!x.completed), shopping=data.shopping.filter(x=>!x.checked);return <ScrollView style={{flex:1}} contentContainerStyle={s.screen} refreshControl={<RefreshControl refreshing={loading} onRefresh={()=>void load()}/>}><Title subtitle="Mały przegląd tego, co dzieje się w domu.">Dzień dobry, {member?.name} ❤️</Title>{stale?<Text style={s.error}>Tryb offline — pokazujemy ostatnie dane.</Text>:null}<Card><Text style={s.label}>Dzisiaj</Text><Text style={s.meta}>{open.filter(x=>x.dueDate?.slice(0,10)===day(new Date())).length} zadań · {data.events.filter(x=>x.startDate.slice(0,10)===day(new Date())).length} wydarzeń</Text></Card><Card><Text style={s.label}>Do zrobienia</Text><Text style={s.meta}>{open.length} otwartych zadań</Text>{open.slice(0,3).map(x=><Text key={x.id}>✓ {x.title}</Text>)}</Card><Card><Text style={s.label}>Zakupy</Text><Text style={s.meta}>{shopping.length} rzeczy do kupienia</Text>{shopping.slice(0,3).map(x=><Text key={x.id}>• {x.name}</Text>)}</Card><Card><Text style={s.label}>Ogarniacze tygodnia</Text>{data.stats?.members.map(x=><Text key={x.id}>{x.name}: {x.count}</Text>)??<Text style={s.meta}>Statystyki pojawią się później.</Text>}</Card><Text onPress={()=>void logout()} style={{...s.error,textAlign:"center",padding:12}}>Wyloguj</Text></ScrollView>}
