import { useState } from "react"; import { Text, View } from "react-native"; import { Redirect } from "expo-router"; import { useAuth } from "../src/AuthProvider"; import { Button, s } from "../src/ui"; import { colors } from "../src/theme";
export default function Login() { const { member, login } = useAuth(); const [error,setError]=useState<string|null>(null); if(member) return <Redirect href="/(tabs)"/>; return <View style={[s.screen,{justifyContent:"center",padding:28}]}><Text style={{fontSize:40,fontWeight:"900",color:colors.ink}}>Wariatkowo</Text><Text style={{fontSize:18,color:colors.muted,marginBottom:24}}>Miśki mieszkają tu razem ❤️</Text><Button title="Zaloguj przez Google" onPress={()=>void login().catch(e=>setError(e.message))}/>{error?<Text style={s.error}>{error}</Text>:null}<Text style={{color:colors.muted,marginTop:12}}>Dostęp mają wyłącznie konta z domowej listy.</Text></View> }
